#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * @n: the integer
 * Return: absolute value of integer.
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);

}
